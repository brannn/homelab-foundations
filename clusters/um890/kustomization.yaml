apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespaces.yaml
  - ../../infrastructure/helm-repositories
  - metallb
  - cert-manager
  - haproxy-ingress
  - monitoring
  - trino
  - nats
  # Longhorn managed by Helmfile (see longhorn/ directory)
  # MinIO managed by Helmfile (see minio/ directory)
